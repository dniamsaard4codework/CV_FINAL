โอเค เดี๋ยวผมทำเป็น “ธนาคารข้อสอบ” ให้เลย แบ่งตามหัวข้อในสไลด์ของอาจารย์นะ
(เน้นแนวคำถามที่ออกได้บ่อย + มีคำตอบสั้นๆ พร้อมจำไปใช้ได้เลย)

---

## 1) Optimization, Neural Network, CNN & Image Classification

### Q1: อธิบาย pipeline การ train โมเดล deep learning สำหรับ image classification

**A:**

1. เตรียม dataset → แบ่ง train / val / test
2. กำหนด model (เช่น linear classifier / MLP / CNN)
3. กำหนด loss function (เช่น cross-entropy)
4. สุ่ม initialize weights
5. ทำ forward pass → ได้ prediction
6. คำนวณ loss
7. ทำ backward pass (backpropagation) → ได้ gradient ของแต่ละ weight
8. ใช้ optimizer (เช่น SGD / Adam) ปรับ weights เพื่อลด loss
9. วน step 5–8 หลาย epoch และ monitor val set เพื่อเช็ค overfitting

---

### Q2: Gradient Descent คืออะไร? ทำงานอย่างไร?

**A:**

* เป็นวิธี optimization ที่ “เดินลงเขา” ตามทิศทางที่ loss ลดลงเร็วที่สุด
* อัปเดตพารามิเตอร์ตามสูตร:
  [
  w := w - \eta \nabla_w J(w)
  ]
  โดยที่ (\eta) = learning rate, (\nabla_w J(w)) = gradient ของ loss w.r.t. w

---

### Q3: แตกต่างระหว่าง Gradient Descent, SGD, Mini-batch SGD

**A:**

* **Full-batch GD**: คำนวณ gradient จากทุกตัวอย่างใน dataset → แม่นแต่ช้า
* **SGD**: ใช้ข้อมูลเพียง 1 ตัวอย่างต่อ step → เร็วมากแต่ noisy มาก
* **Mini-batch SGD**: ใช้ batch ขนาด B (เช่น 32, 64) → trade-off ความแม่นกับความเร็ว, ใช้จริงบ่อยที่สุด

---

### Q4: ถ้า learning rate เล็กเกินไป / ใหญ่เกินไป จะเกิดอะไรขึ้น?

**A:**

* เล็กไป → loss ลดช้า, train นาน, เสี่ยงติด local minima
* ใหญ่ไป → ข้าม minima, loss แกว่งหรือ diverge
  → จึงมีเทคนิค **learning rate decay** (step, cosine, linear, inverse sqrt ฯลฯ) และ **adaptive LR** เช่น Adam, Adagrad ฯลฯ

---

### Q5: Backpropagation ทำอะไร? ใช้ chain rule อย่างไร?

**A:**

* เป้าหมาย: หา (\frac{\partial J}{\partial w_i}) สำหรับทุก weight
* ใช้ **chain rule** ย้อนจาก output layer → hidden layers → input
* ตามกราฟ computation: คำนวณ gradient ที่ปลายสุดก่อน แล้ว propagate ย้อนกลับผ่านแต่ละ node

---

### Q6: Activation function มีหน้าที่อะไร? ถ้าไม่มี activation เลยจะเกิดอะไร?

**A:**

* หน้าที่: ใส่ non-linearity ทำให้ network สามารถ approximate ฟังก์ชันซับซ้อน
* คุณสมบัติที่ดี: differentiable, คำนวณง่าย, มัก monotonic
* ถ้าไม่มี activation (ทุก layer เป็น linear) → ทั้ง network = linear transformation เดียว → สมมติฐานแบบ linear classifier ยังเหมือนเดิม แม้มีหลาย layer ก็ไม่ช่วยอะไร

---

### Q7: ทำไม CNN ดีกว่า fully-connected (MLP) สำหรับภาพ?

**A:**

* FC layer มอง input เป็นเวกเตอร์ 1D → ไม่ใช้โครงสร้างเชิงพื้นที่ของภาพ
* จำนวนพารามิเตอร์สูงมาก (เชื่อมทุก pixel กับทุก neuron)
* CNN ใช้ **convolution filter** ที่:

  * shared weights → ใช้ filter เดียวกันสแกนทั้งภาพ
  * local receptive field → โฟกัสบริเวณเล็กๆ และค่อยๆ รวมเป็น feature ใหญ่
    → ทำให้ใช้พารามิเตอร์น้อยกว่า, ใช้ pattern ที่ซ้ำกันในภาพ, เหมาะกับงาน vision

---

### Q8: คำนวณขนาด output ของ convolution layer

ให้ Input size = (W), filter size = (K), padding = (P), stride = (S)
**A:**
[
W_\text{out} = \frac{W - K + 2P}{S} + 1
]

ตัวอย่าง: Input 32x32, filter 5x5, P=2, S=1 →
[
(32 - 5 + 2\times2)/1 + 1 = 32
]
ถ้ามี 10 filters → output volume = 10 × 32 × 32 

---

### Q9: Receptive field คืออะไร? เพิ่มขึ้นอย่างไรเมื่อลง stack conv หลายชั้น?

**A:**

* Receptive field = ขนาดบริเวณใน input ที่มีผลต่อค่า pixel หนึ่งใน feature map
* ถ้าใช้ conv kernel size = K, ไม่มี stride
* ด้วย L layers → receptive field size = (1 + L (K - 1))
  เช่น L=2, K=3 → RF = 5x5 

---

## 2) Object Detection (R-CNN, Fast/Faster R-CNN, IoU, mAP, NMS, YOLO/SSD) 

### Q10: Object detection ต่างจาก image classification ยังไงบ้าง?

**A:**

* Classification: รับภาพ → ทาย “คลาสเดียว” (ไม่มีข้อมูลตำแหน่ง)
* Detection:

  * ต้องทาย “หลาย object” พร้อมกัน
  * แต่ละ object ต้องให้ทั้ง **class label** และ **bounding box (x,y,w,h)**
  * จัดการ object คนละขนาด/ตำแหน่ง/จำนวน ที่ไม่แน่นอน

---

### Q11: นิยาม IoU (Intersection over Union) และใช้ทำอะไร?

**A:**

* ให้กล่องทำนาย = (B_p), กล่อง ground truth = (B_{gt})
  [
  IoU = \frac{\text{พื้นที่ส่วนที่ซ้อนกัน (intersection)}}{\text{พื้นที่ผลรวม (union)}}
  ]
* ใช้เพื่อ:

  * ตัดสินว่า detection “ตรง” กับ GT หรือไม่ (เช่น IoU > 0.5 ถือว่าถูก)
  * ใช้ในขั้น NMS และ metric อย่าง mAP 

---

### Q12: อธิบายขั้นตอนหลักของ R-CNN (training + test-time)

**A:**
**Train:**

1. ใช้ region proposal (เช่น Selective Search) สร้าง ~2000 proposals / image
2. Crop region → resize เป็น 224×224 → ส่งเข้า CNN (pretrained ImageNet)
3. Extract feature (เช่น fc7) แล้ว train SVM แยกคลาส (C+1 classes: แต่ละคลาส + background)
4. Train regressor สำหรับปรับ bounding box (box regression)

**Test:**

1. รัน region proposal กับภาพใหม่
2. crop & resize แต่ละ region → ส่งเข้า CNN → ได้ feature
3. SVM → class scores, regressor → box refine
4. ใช้ NMS กำจัดกล่องซ้อนกันมากเกินไป

ข้อเสีย: ต้อง forward CNN ~2000 ครั้ง / รูป → ช้ามาก 

---

### Q13: Fast R-CNN แก้ปัญหาอะไรของ R-CNN? หลักการคืออะไร?

**A:**
ปัญหา: R-CNN ต้องรัน CNN ต่อ region → ช้า

หลักการ Fast R-CNN:

1. รัน CNN กับทั้งภาพ “ครั้งเดียว” → ได้ feature map
2. Project region proposals ลงบน feature map
3. ใช้ **ROI Pool** (หรือ ROI Align) crop แพตช์ feature แต่ละ region เป็นขนาดคงที่ เช่น 7×7
4. ส่งแต่ละ ROI เข้า fully-connected layers → ทาย class + box refinement

ประหยัดเวลาเพราะ share convolution features ใช้ร่วมกันทั้งภาพ 

---

### Q14: ROI Pool vs ROI Align ต่างกันยังไง?

**A:**

* **ROI Pool**

  * quantize พิกัด bounding box เป็น grid integer
  * ทำ max-pooling ในแต่ละ bin → เกิด **quantization error** (ตำแหน่ง feature shift เล็กน้อย)
* **ROI Align**

  * ไม่ quantize เป็น integer
  * ใช้ bilinear interpolation เพื่ออ่านค่าจุด float ใน feature map
  * ลด misalignment, ให้ localization แม่นยำขึ้น

---

### Q15: Faster R-CNN เพิ่มอะไรจาก Fast R-CNN? Region Proposal Network (RPN) คืออะไร?

**A:**

* Fast R-CNN ยังใช้ “Selective Search” บน CPU → ช้า
* Faster R-CNN เพิ่ม **RPN** ซึ่งเป็น CNN เล็กบน feature map เดิม เพื่อ **predict region proposals** เอง
* ที่แต่ละตำแหน่งของ feature map เราวาง **anchor boxes** หลายสเกล/อัตราส่วน แล้ว RPN ทำนาย

  * objectness score (เป็น object หรือไม่)
  * box regression ของ anchor นั้น
* จากนั้นเอา proposals ที่ได้ไปใช้ ROI Pool / ROI Align ต่อเหมือน Fast R-CNN

---

### Q16: อธิบายขั้นตอนของ Non-Maximum Suppression (NMS)

**A:**
ให้ detections ทั้งหมดของคลาสหนึ่ง:

1. เรียงกล่องตาม score จากมาก → น้อย
2. เลือกกล่องที่ score สูงสุด → เก็บไว้เป็นผลลัพธ์
3. ลบกล่องอื่นที่มี IoU กับกล่องนี้ > threshold (เช่น 0.7)
4. ทำซ้ำขั้นตอน 2–3 จนไม่มีกล่องเหลือ

NMS ช่วยลดการทำนายซ้ำของ object เดียวกัน 

---

### Q17: mAP (mean Average Precision) สำหรับ object detection คำนวณอย่างไร?

**A:**
สำหรับแต่ละคลาส:

1. รัน detector บนทุกภาพ (พร้อม NMS)
2. เรียง detection ตาม score จากมาก → น้อย
3. แต่ละ detection:

   * ถ้า match กับ GT (IoU > 0.5) และ GT นั้นยังไม่ถูกใช้ → นับเป็น True Positive
   * ถ้าไม่ → False Positive
4. คำนวณ Precision, Recall ทีละจุด → ได้ PR curve
5. **Average Precision (AP)** = พื้นที่ใต้ PR curve
6. **mAP** = ค่าเฉลี่ยของ AP ทุกคลาส

* COCO mAP: เฉลี่ย AP ที่ IoU หลาย threshold (0.5, 0.55, …, 0.95) 

---

## 3) Object Tracking & Change Detection

### Q18: ปัญหา “Object Tracking” นิยามว่าอะไร? ต้องรู้อะไรบ้าง?

**A:**

* ให้ลำดับภาพ (video) เป็นฟังก์ชันของตำแหน่ง (x,y) และเวลา t
* เป้าหมาย: estimate จำนวนและ **state** ของ object ใน region ที่สนใจตลอดเวลา
* state อาจประกอบด้วย: ตำแหน่ง, ความเร็ว, ขนาด bounding box, appearance descriptor ฯลฯ
* แบ่งได้เป็น

  * Single-target tracking
  * Detection + tracking (0 หรือ 1 object)
  * Multi-target detection and tracking (หลาย object)

---

### Q19: ทำไมต้องมี tracking ในเมื่อเรามี detector อยู่แล้ว?

**A:**

* Detector อาจพลาด object ในบาง frame เพราะ

  * occlusion
  * blur / แสงเปลี่ยน / viewpoint เปลี่ยนชั่วคราว
  * background clutter
* Trackingช่วย

  * fill-in การ detect ที่หายไปช่วงสั้นๆ
  * ใช้ motion model ทาย trajectory ในอนาคต (เช่น เดินจะข้ามถนนไหม) 

---

### Q20: องค์ประกอบหลักของ tracking มีอะไรบ้าง?

**A:**

1. **Detection** – หา candidate objects ในแต่ละ frame
2. **Data Association** – จับคู่ detection ใน frame ปัจจุบันกับ track ที่มีอยู่ (ตัวเดียวกันไหม)
3. **Prediction** – ใช้ motion model ทายตำแหน่งในอนาคต เพื่อช่วย association และ handle missing detections 

---

### Q21: อธิบาย Simple Background Subtraction และข้อจำกัด

**A:**

* มี background model เป็นภาพ “ฉากนิ่ง” (ไม่มีวัตถุ)
* สำหรับแต่ละ frame:

  * คำนวณ |I_t(x,y) – B(x,y)|
  * threshold → foreground (มีการเปลี่ยน) vs background (นิ่ง)
    **ข้อจำกัด**
* ถ้าสิ่งของเข้ามาแล้วหยุด → จะถูก detect ตลอดไป (ghost, outdated background)
* ถ้า background เดิมเริ่มขยับ → ทั้ง object ใหม่และส่วนที่เผย background จะถูก detect
* แพ้ illumination change, shadow, tree swaying ฯลฯ
* global threshold เดียวอาจไม่ดีทั้งภาพ 

---

### Q22: Frame differencing ต่างจาก background subtraction ยังไง? ข้อดี/ข้อเสีย?

**A:**

* ใช้ frame t เทียบกับ frame t-1 เป็น background แทนภาพอ้างอิงคงที่
  **ข้อดี**
* ปรับตัวเร็วต่อแสง/กล้องสั่น
* วัตถุที่หยุดนิ่งจะหายไปจาก foreground (ไม่เป็น ghost)
  **ข้อเสีย**
* ตรวจเจอแค่ “ขอบนำ/ท้าย” ของ object → ได้ pixel foreground น้อย
* วัตถุที่เคลื่อนเข้าหากล้อง/ห่างกล้องตรวจจับยาก 

---

### Q23: Adaptive Background Subtraction (exponential moving average) ทำงานอย่างไร? trade-off ของพารามิเตอร์ α คืออะไร?

**A:**

* update background ตามสูตร (ต่อ pixel):
  [
  B_t = (1-\alpha)B_{t-1} + \alpha I_t
  ]
* (\alpha = 0) → background fixed (เหมือน simple subtraction)
* (\alpha = 1) → เท่ากับ frame differencing
  **Trade-off:**
* α สูง: ปรับตัวเร็วต่อแสง/กล้องสั่น, แต่ object ใหญ่-ช้า จะค่อยๆ ละลายกลายเป็น background
* α ต่ำ: object/ghost หายช้า แต่ background เสถียรมากขึ้น 

---

### Q24: ทำไมต้องใช้ Gaussian Mixture Model (GMM) ใน background modeling?

**A:**

* intensity ของ pixel เดียวเมื่อดูนานๆ อาจเป็น **หลายโหมด** เช่น

  * ถนน (ค่าคงๆ)
  * หิมะตก (noise)
  * รถผ่านบางครั้ง
* ให้ pixel หนึ่งมี distribution เป็น **mixture ของ K Gaussian** แต่ละตัวมี weight, mean, variance
* Gaussians ที่มี weight สูงและ variance ต่ำ (อยู่บ่อย / ค่อนข้างนิ่ง) → ถือเป็น background
* Gaussians ที่ weight เล็ก → foreground

ช่วย handle background ที่มีหลายสถานะ เช่น tree swaying, noise, shadow 

---

### Q25: Tracking-by-detection คืออะไร? ยกตัวอย่างวิธี feature-based เช่น SIFT tracking

**A:**

* **Tracking-by-detection**: ทุก frame เรา detect/หา feature ก่อน แล้วค่อยจับคู่ (match) เพื่อ track object
* ตัวอย่าง SIFT-based tracking:

  1. Frame 1: user เลือก bounding box เป็น object
  2. สกัด SIFT features ในกล่อง (object set O) และ background (set B)
  3. Frame t: สกัด SIFT ในทั้งภาพ
  4. สำหรับแต่ละ feature: หา distance ดีที่สุดกับ O และกับ B → คำนวณ score ว่าเหมือน object แค่ไหน
  5. สำหรับทุก search window: รวม score ของ feature ข้างใน + penalty ถ้าเบี่ยงจากตำแหน่ง/ขนาดเดิมมากไป
  6. เลือก window ที่ score สูงสุดเป็นตำแหน่ง object ใหม่ แล้ว update object model ต่อไป 

---

### Q26: GOTURN (learning-based tracker) มีข้อดี/ข้อเสียอย่างไร?

**A:**

* ใช้ CNN regression network ที่รับ

  * crop ของ object จาก frame t-1
  * crop search area จาก frame t
    → ทำนาย bounding box ของ object ใน frame t 
    **ข้อดี:**
* ไม่ต้อง train online ต่อ object ใหม่ทุกตัว → เร็ว (100 FPS)
* คล้าย template matching แต่เรียน feature เองด้วย deep network
  **ข้อเสีย:**
* สมมติ motion ค่อนข้าง smooth → ถ้า object กระโดดเร็วออกนอก search window → track หายแล้วกู้คืนยาก

---

## 4) Image Segmentation (Classical + Deep)

### Q27: Image segmentation ต่างจาก classification / detection อย่างไร?

**A:**

* **Classification**: ทาย label เดียวของทั้งภาพ
* **Detection**: ทาย bounding boxes + label ของแต่ละ object
* **Segmentation**: ทาย label **ทุก pixel** ในภาพ

  * **Semantic segmentation**: pixels ที่เป็น object ประเภทเดียวกัน แชร์ label เดียวกัน (ไม่แยก instance)
  * **Instance segmentation**: แยก instance แต่ละตัวของคลาสเดียวกัน (คน 3 คน = 3 instance)
  * **Panoptic**: รวม semantic + instance

---

### Q28: แนวคิด “segmentation as clustering” คืออะไร? pixel feature vector ประกอบด้วยอะไรได้บ้าง?

**A:**

* มองแต่ละ pixel เป็นจุดหนึ่งใน **feature space** เช่น
  [
  f = [R, G, B, x, y, d, \dots]
  ]
* Pixel ที่คล้ายกัน (ระยะ Euclidean ใกล้กัน) ควรอยู่ cluster เดียวกัน
* ใช้ clustering algorithm เช่น k-means / mean shift แบ่งกลุ่ม pixels เป็น segments 

---

### Q29: เขียนขั้นตอน k-means clustering สำหรับ segmentation และข้อดี/ข้อเสีย

**ขั้นตอน:** 

1. เลือกจำนวน cluster = k
2. Initialize centroid (m_1, …, m_k) แบบสุ่มหรือวิธีอื่น
3. Assign: ให้ pixel แต่ละตัวไปอยู่ cluster ที่ centroid ใกล้สุด
4. Update: คำนวณ mean ใหม่ของแต่ละ cluster (ค่าเฉลี่ย feature ของ pixels ใน cluster นั้น)
5. ทำ step 3–4 ซ้ำจน centroid ขยับน้อยกว่าค่า threshold

**ข้อดี:**

* ง่าย, เร็ว, implement ไม่ยาก
  **ข้อเสีย:**
* ต้องกำหนด k ล่วงหน้า
* sensitive ต่อ initialization
* sensitive ต่อ outlier
* ไม่ enforce spatial smoothness → pixel คนละที่แต่อยู่สีเดียวกันอาจอยู่ cluster เดียวกัน

---

### Q30: Mean Shift segmentation ทำงานบน density อย่างไร? แตกต่างจาก k-means ยังไง?

**A:** 

* มอง distribution ของ points ใน feature space แล้วพยายามหา “mode” (จุดสูงสุดของ density)
* สำหรับ mean shift:

  1. เริ่มที่จุดหนึ่ง (initial mean)
  2. วาง window (kernel) รอบจุดนี้
  3. หาค่าเฉลี่ยของจุดใน window → เลื่อน mean เข้าไปหาศูนย์กลาง density
  4. ทำซ้ำจน mean แทบไม่ขยับ → ได้ mode
* Points ที่ converge ไป mode เดียวกัน → อยู่ cluster เดียวกัน

**ต่างจาก k-means:**

* ไม่ต้องกำหนด k ล่วงหน้า (จำนวน cluster เกิดเองจาก density)
* handle รูปทรง cluster ได้หลากหลาย (ไม่จำกัดเป็นวงกลม/convex)
* แต่คำนวณช้า, ต้องเลือกขนาด window ที่เหมาะสม

---

### Q31: Graph-based segmentation ด้วย Min-Cut / Normalized Cut คืออะไร?

**A:** 

* สร้าง **กราฟ**:

  * แต่ละ pixel = vertex
  * มี edge เชื่อม pixel ใกล้ๆ กัน (หรือทุกคู่)
  * น้ำหนัก edge = affinity (ความคล้ายของ pixel สองตัว)
* Min-cut: แบ่งกราฟเป็นสองส่วน (A,B) เพื่อลดผลรวม weight ของ edges ที่ถูกตัด

  * แต่จะ bias ให้ตัดกลุ่มเล็กๆ (เพราะ cost น้อย)
* Normalized Cut (NCut): เพิ่ม term normalization คำนึงถึง “ความใหญ่ของ subgraph”

  * ลด bias ต่อ partition เล็ก
  * แต่แก้แบบ exact เป็น NP-complete → ใช้ eigenvector-based approximation

---

### Q32: Semantic Segmentation ด้วย Fully Convolutional Network (FCN) ต่างจาก sliding window อย่างไร?

**A:** 

* **Sliding window**:

  * ตัด patch เล็กๆ รอบแต่ละ pixel → ส่งเข้า classifier
  * ไม่ reuse feature ของ patch ที่ overlap กัน → ช้ามาก
* **FCN**:

  * ใช้ conv layers ทั้งหมด (ไม่มี FC ต้นฉบับ)
  * รับภาพทั้งภาพ → สร้าง feature map → upsample กลับไป resolution สูง แล้ว output per-pixel score map
  * share computation บน feature map → เร็วและประหยัดกว่า

---

### Q33: วิธี upsampling ที่ใช้ใน semantic segmentation มีอะไรบ้าง? แตกต่างกันยังไง?

**A:** 

* **Unpooling**

  * ใช้ “switch” จาก max-pooling เพื่อวางค่าที่ตำแหน่งที่เคย max
* **Interpolation**

  * Nearest neighbor / bilinear / bicubic → ไม่เรียนรู้พารามิเตอร์, purely geometric
* **Transposed Convolution (Deconv)**

  * convolution transpose ที่เรียนรู้ kernel ได้
  * คล้าย learnable upsampling → ขยาย resolution พร้อมเรียนรู้องค์ประกอบของภาพ

---

### Q34: Transposed Convolution คืออะไร (เชิง intuition)?

**A:**

* ปกติ convolution: ลดขนาด (downsample) เมื่อใช้ stride > 1
* Transposed convolution: ทำกระบวนการ “กลับด้าน”

  * input size เล็ก → output size ใหญ่
  * kernel ถูก spread ออกไปใน output space แล้วค่าซ้อนกันถูก sum
* ใช้เป็น layer สำหรับ learnable upsampling ใน decoder ของ segmentation / generator ใน GAN ฯลฯ 

---

## 5) 3D Vision & 3D Deep Learning

### Q35: 2.5D (Depth Map) ต่างจาก full 3D representation ยังไง?

**A:**

* Depth map: สำหรับแต่ละ pixel ในภาพ บอก “ระยะ” จากกล้องถึงพื้นผิว

  * แสดงแค่พื้นผิวที่มองเห็นจากมุมมองเดียว (visible surface) → เลยเรียก 2.5D
* Full 3D: แสดง shape ทั้งวัตถุใน space (รวมด้านที่กล้องมองไม่เห็น) เช่น point cloud, mesh, voxel

---

### Q36: ยกตัวอย่าง 3D representations หลักๆ พร้อมข้อดี/ข้อเสีย

**A:** 

* **Point Cloud**

  * ข้อดี: ใกล้กับ raw sensor (LiDAR), เรียบง่าย, flexible, scalable
  * ข้อเสีย: ไม่มี connectivity ชัดเจน, render ให้เรียบเนียนยาก
* **Polygon Mesh** (vertices + faces)

  * ข้อดี: แสดงผิวอย่างชัดเจน, render สวย, มีการ up/down-sampling ได้
  * ข้อเสีย: โครงสร้างซับซ้อน, แก้ topology ยาก
* **Voxel Grid**

  * ข้อดี: โครงสร้างเป็น grid ง่ายต่อการใช้ 3D CNN, แทน shape ได้ทั่วไป
  * ข้อเสีย: memory สูงมากเมื่อ resolution สูง
* **Implicit (SDF, level set)**

  * แสดง shape ผ่านฟังก์ชัน f(p)=0 (surface), f(p) < 0 / >0 = inside / outside
  * ข้อดี: inside–outside test ง่าย, topological change ทำได้สวย
  * ข้อเสีย: sampling / rendering ยุ่งยากกว่า

---

### Q37: Point cloud “organized” vs “unorganized” คืออะไร?

**A:** 

* **Organized**: points ถูกจัดเป็นตาราง (rows, columns) เช่น depth image จาก RGB-D camera

  * รู้ว่าจุดไหนเป็นเพื่อนบ้านกันในภาพ → เร็วต่อบาง algorithm
* **Unorganized**: points เป็น list คละๆ ไม่เป็น grid (เช่น LiDAR scan)

  * ต้องใช้โครงสร้างข้อมูลเพิ่ม (kd-tree ฯลฯ) เพื่อหาข้างเคียง

---

### Q38: Point Cloud มีข้อดี-ข้อจำกัดอะไรบ้าง?

**A:** 
**ข้อดี:**

* ใกล้ raw sensor, ได้มาง่ายจาก 3D scanner ราคาถูก
* โครงสร้างเรียบง่าย (แค่จุดจำนวนมาก)
  **ข้อจำกัด:**
* ไม่มีข้อมูลหน้า/หลัง (connectivity)
* smoothing / rendering สวยๆ ต้องทำ mesh reconstruction
* tasks บางอย่าง (เช่น inside–outside test) ยากกว่า implicit

---

### Q39: อธิบาย difference ระหว่าง explicit vs implicit 3D representation

**A:** 

* **Explicit**: เก็บจุดของพื้นผิว/วัตถุโดยตรง เช่น point cloud, mesh, voxel occupancy

  * sampling ง่าย (เรามีจุดแล้ว)
  * แต่การถาม “จุดนี้อยู่ในวัตถุหรือเปล่า” (inside/outside) อาจยาก
* **Implicit**: แทนวัตถุด้วยสมการหรือฟังก์ชัน f(x,y,z)

  * surface = {p | f(p)=0} (เช่น sphere: (x^2 + y^2 + z^2 - r^2 = 0))
  * inside/outside test ง่าย: sign ของ f
  * แต่ sampling surface ให้ได้ mesh สวยๆ ต้องใช้ algorithm เพิ่ม (marching cubes ฯลฯ)

---

### Q40: Signed Distance Function (SDF) คืออะไร?

**A:** 

* เป็น implicit function f(p) ที่ให้ค่า = ระยะ signed จากจุด p ถึงพื้นผิว

  * f(p) = 0 บน surface
  * f(p) < 0 ด้านใน
  * f(p) > 0 ด้านนอก
* ทำให้เรารู้ทั้ง inside/outside และระยะถึงผิวในคราวเดียว → ใช้ใน reconstruction / rendering / 3D deep learning

---

### Q41: อธิบาย pipeline คร่าวๆ ของ point cloud registration

**A:** 

1. Detect keypoints ที่มีความสำคัญ (เช่น มุม, ขอบ) – คุณสมบัติ: repeatable, distinctive
2. Estimate feature descriptor รอบ keypoint (เช่น PFH, VFH)
3. Matching descriptors ระหว่างสอง point clouds → ได้ candidate correspondences
4. ใช้ RANSAC เพื่อ reject outliers และ estimate transformation คร่าวๆ
5. Refinement ด้วย ICP ฯลฯ ให้ alignment เนียน
6. ใช้ผลเพื่อ fusion, mapping, pose estimation ฯลฯ

---

### Q42: PointNet แก้ปัญหาอะไรใน 3D deep learning?

**A:** 

* ปัญหา: point cloud เป็นชุดจุดที่ **unordered** (สลับลำดับจุดแล้ว shape เดิม)
* PointNet ออกแบบ network ที่

  * ใช้ MLP กับแต่ละจุด (shared weights)
  * ใช้ symmetric function (เช่น max pooling) aggregate เป็น global feature → invariant ต่อ permutation
* สามารถเรียนรู้ feature จาก point cloud โดยตรง สำหรับ classification, segmentation, part labeling ฯลฯ

---

## 6) Generative Models (AE, VAE, GAN) – แนวคำถามยอดฮิต

*(ส่วนนี้อิงความรู้ทั่วไปของวิชา ไม่ได้อ้างจากไฟล์)*

### Q43: Autoencoder คืออะไร? ใช้ทำอะไร?

**A:**

* neural network ที่มีสองส่วน: encoder + decoder
* Encoder: map input x → latent z
* Decoder: map z → reconstruct (\hat{x})
* Train ด้วย objective ให้ reconstruction error ต่ำ (เช่น MSE ระหว่าง x กับ (\hat{x}))
* ใช้สำหรับ: dimensionality reduction, denoising, feature learning ฯลฯ

---

### Q44: VAE ต่างจาก AE ตรงไหน? ทำไมเรียกว่า “variational”?

**A:**

* แทนที่จะ map x → z เดี่ยวๆ, VAE ให้ encoder ทำนายพารามิเตอร์ของ distribution (q_\phi(z|x)) (เช่น Gaussian: mean, log-variance)
* ใช้ reparameterization trick: (z = \mu + \sigma \odot \epsilon, \epsilon \sim \mathcal{N}(0, I))
* loss = reconstruction loss + KL divergence ระหว่าง (q_\phi(z|x)) กับ prior (p(z)) (เช่น N(0, I))
* ทำให้ latent space “smooth” และ generative ได้ (sample z จาก prior → decode เป็นตัวอย่างใหม่)

---

### Q45: GAN ประกอบด้วยอะไรบ้าง? Train อย่างไร?

**A:**

* Two networks:

  * Generator G(z) – สร้างภาพปลอมจาก noise z
  * Discriminator D(x) – แยกว่าภาพนี้จริง (จาก data) หรือปลอม (จาก G)
* Objective แบบ minimax:

  * D พยายาม maximize ความสามารถในการแยกจริง/ปลอม
  * G พยายาม generate ให้ D เชื่อว่าเป็นจริง
* ผลลัพธ์: G เรียนรู้ distribution ของ data → สร้างตัวอย่างใหม่ที่เหมือน data จริง

---

ถ้าอยาก ผมสามารถต่อยอดให้เป็น **ข้อสอบแบบคำนวณ** เช่น

* คำนวณ output size ของ conv หลายชั้น
* คำนวณ IoU ของสองกล่อง
* ออกแบบ k-means segmentation ใน feature space [R,G,B,x,y]
* หรือโจทย์ conceptual ยาวๆ (ถามอธิบาย pipeline ของ Faster R-CNN แบบเต็ม)

แต่ชุดด้านบนครอบคลุม “แกนหลัก” ที่ออกบ่อยเกือบทุกข้อแล้ว 👍
อยากให้ผมเปลี่ยนชุดนี้เป็น **flashcard สั้นๆ** ไว้อ่านก่อนเข้าห้องสอบก็สั่งได้เลย
